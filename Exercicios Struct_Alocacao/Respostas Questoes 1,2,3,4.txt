-----------
|Respostas|
-----------
Questão 1:

Media de Maria: 92.000000
Media de ð@: 90.000000

-----------
Questão 2:

A lista de notas do aluno 2 (a2) aponta diretamente para a lista de notas do aluno 1 (a1), ou seja, os dois estão conectados. Então se por acaso for alterado quaisquer valores da lista de notas do aluno 1 (a1), os efeitos surtirão também na lista de notas do aluno 2 (a2) e vice-versa.
O certo seria fazer um novo loop "for" exclusivo para lista de notas do a2, alterando seus valores individualmente, assim como foi feito no a1, garantindo assim, duas listas separadas e distintas, sem relação.

-----------
Questão 3:

Devido a conectividade entre as duas listas, ao usar o free() no aluno 1 (a1), possivelmente ao tentar usar a lista de alunos 2 (a2) posteriormente poderia gerar um erro, já que agora a lista de a2 estaria vazia.

-----------
Questão 4:
______________________________________________________
/************************************************************************************************
 * Instituto Federal de Educação Ciência e Tecnologia da Paraíba
 * Campus Campina Grande
 * Curso Superior de Bacharel em Engenharia de Computação
 * Componente Curricular: Laboratório de Estrutura de Dados e Algoritmos - 2º Período
 * Professor: Ruan Delgado
 * 
 * Copyright - Guilherme Esdras
 ************************************************************************************************/

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef struct {
    int idade;
    char nome[50];
    int *notas;
} aluno;

int main() {
    aluno a1, a2;
    a1.idade = 10;
    strcpy(a1.nome, "Pedro");
    a1.idade = 12;
    strcpy(a1.nome, "Maria");

    a1.notas = (int*) malloc(5*sizeof(int));
    a2.notas = (int*) malloc(4*sizeof(int));

    int i;
    for( i = 0; i < 5; i++ ) {
        a1.notas[i] = 100;
    }
    for( i = 0; i < 4; i++ ) {
        a2.notas[i] = a1.notas[i] - 10;
    }
    double media = 0;
    for(i = 0; i < 5; i++) {
        media += a1.notas[i];
    }
    media = media/5;
    printf("Media de %s: %lf\n", a1.nome, media);

    media = 0;
    for(i = 0; i < 4; i++) {
        media += a2.notas[i];
    }
    media = media/4;
    printf("Media de %s: %lf\n", a2.nome, media);

    free(a1.notas);
    free(a2.notas);

    return 0;
}
______________________________________________________
Saída:

Media de Maria: 100.000000
Media de ð@: 90.000000

==========================================================================

